dumptitle = Program(source=["dumptitle.c", "flip.c", ])
romutil = Program(source=["romutil.c", "flip.c", ],)
sampleutil = Program(source=["readlist.c", "flip.c",], LIBS = ["sndfile", ])

unrom = Program(source=["unrom.c",], LIBS = ["sndfile", ])

